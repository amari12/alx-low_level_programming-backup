#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *main - prints program name
 *@argc: number of arguments
 *@argv: arguments array
 *Return: void
 */

int main(int argc, char *argv[])
{
(void) argc;
char name[] = argv[0];

printf("%s\n", name);

return (0);
}
